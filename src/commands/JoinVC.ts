import { CommandInteraction, Client } from "discord.js";
import { Command } from "../Command";

export const JoinVC: Command = {
    name: "joinvc",
    description: "To join Voice Channel",
    run: async (client: Client, interaction) => {
       
    }
};

